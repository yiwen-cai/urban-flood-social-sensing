# 课程分析简报 — 2018年 Kerala 洪水社交媒体案例

> 生成时间：{{ generated_at }}
> 数据来源：HumAID `kerala_floods_2018` test split（{{ unique_posts }} 条唯一帖子）
> 选用模型：`{{ model_version or "（无模型预测）" }}`
> 证据状态：📄 数据集记录 / 👤 人工标签 / 🤖 模型预测
> **本简报用于课程技术演示，仅归纳冻结数据中的可见讨论，不代表真实灾情、总体公众需求或权威结论。**

---

## 一、概述

2018年8月，印度 Kerala 邦遭遇百年一遇的特大洪水。本简报基于 HumAID 数据集中 Kerala 洪水事件的 {{ unique_posts }} 条唯一帖子，通过 baseline 与 LLM 分类器对比识别九类人道信息，并完成小样本探索性情绪分析。项目仅验证技术流程和课程概念，不评价真实救援效果。

---

## 二、数据范围与质量

| 维度 | 数值 |
|------|------|
| 数据来源 | HumAID `kerala_floods_2018` test split |
| 唯一帖子数 | {{ unique_posts }} |
| 有参考标签 | {{ metrics.records_with_reference_label }} |
| 有探索性情绪标注 | {{ metrics.records_with_emotion }} |
| 模型版本 | {{ metrics.model_versions | join(", ") }} |
| 时间信息 | 无逐帖时间（字段为 null） |
| 地点信息 | 无逐帖地点（字段为 null） |
| 脱敏处理 | 账号 → `[USER]`，号码 → `[NUMBER]`，邮箱 → `[EMAIL]` |
{% if model_metrics %}
| 覆盖率（选用模型） | {{ "%.2f"|format(model_metrics.coverage * 100) }}% |
| 准确率（全量分母） | {{ "%.2f"|format(model_metrics.accuracy * 100) }}% |
| Macro-F1 | {{ "%.4f"|format(model_metrics.macro_f1) }} |
{% endif %}

---

## 三、人道信息类别与模型评估

### 3.1 九类人道信息分布（唯一帖子）

| 类别 | 数量 | 占比 | 精确率 | 召回率 | F1 |
|------|------|------|--------|--------|-----|
{%- for label in labels %}
| {{ label_name_map[label] }} | {{ ref_dist.get(label, 0) }} | {{ "%.1f"|format((ref_dist.get(label, 0) / unique_posts) * 100) }}% | {{ "%.3f"|format((model_metrics.per_class or {}).get(label, {}).get("precision", 0)) }} | {{ "%.3f"|format((model_metrics.per_class or {}).get(label, {}).get("recall", 0)) }} | {{ "%.3f"|format((model_metrics.per_class or {}).get(label, {}).get("f1", 0)) }} |
{%- endfor %}

### 3.2 关键类别关注

{% set urgent = (model_metrics.per_class or {}).get("requests_or_urgent_needs", {}) %}
- **紧急需求类**：参考支持 {{ urgent.get("support", ref_dist.get("requests_or_urgent_needs", 0)) }} 条，召回率 {{ "%.1f"|format((urgent.get("recall", 0) * 100)) }}%。
{% set rescue = (model_metrics.per_class or {}).get("rescue_volunteering_or_donation_effort", {}) %}
- **救援与捐赠类**：参考支持 {{ rescue.get("support", ref_dist.get("rescue_volunteering_or_donation_effort", 0)) }} 条，占比 {{ "%.1f"|format((ref_dist.get("rescue_volunteering_or_donation_effort", 0) / unique_posts) * 100) }}%。

---

## 四、代表性脱敏记录

以下为选用模型下各类别中置信度最高的代表性记录。真实来源模式不展示原文正文，仅保留 `post_id` / `source_ref` 与选择理由。

{%- for rec in evidence_records %}
{%- if rec.selection_reason.startswith("top-") %}
### {{ label_name_map.get(rec.predicted_label, rec.predicted_label) }}

- post_id：`{{ rec.post_id }}`
- 模型：`{{ rec.model_version }}`
- 选择理由：{{ rec.selection_reason }}
{% if rec.text_clean %}- 原文摘要：{{ rec.text_clean }}
{% else %}- 原文摘要：（真实数据模式不展示正文）
{% endif %}- 情感标签：{{ emotion_name_map.get(rec.exploratory_emotion, rec.exploratory_emotion or "无") }}
- 置信度：{{ "%.2f"|format(rec.confidence or 0) }}
- 来源：`{{ rec.source_ref }}`
{%- endif %}
{%- endfor %}

### 紧急需求证据（全量纳入）

{%- set urgent_rows = evidence_records | selectattr("selection_reason", "equalto", "urgent needs: all records included") | list %}
{%- if urgent_rows %}
共 {{ urgent_rows | length }} 条紧急需求证据进入简报。
{%- for rec in urgent_rows[:10] %}
- `{{ rec.post_id }}` / `{{ rec.model_version }}` / conf={{ "%.2f"|format(rec.confidence or 0) }}
{%- endfor %}
{%- if urgent_rows | length > 10 %}
- … 其余 {{ urgent_rows | length - 10 }} 条略
{%- endif %}
{%- else %}
当前选用模型下无紧急需求证据。
{%- endif %}

---

## 五、可见讨论中的信息缺口

{%- for label in gap_labels %}
- **{{ label_name_map[label] }}** 仅 {{ min_count }} 条记录（{{ "%.1f"|format((min_count / unique_posts) * 100) }}%）。
{%- endfor %}

> 社交媒体数据不构成对灾情的完整抽样。"信息缺口"仅表示冻结数据中该类别可见讨论较少。

---

## 六、探索性情绪分析

> 情绪标签为课程成员人工标注的小样本探索性实验；分母使用已标注子集（{{ emotion_n }}），不是全部 {{ unique_posts }} 帖。

| 情绪类别 | 数量 | 占比（已标注子集） |
|----------|------|--------------------|
{%- for emotion in emotions %}
| {{ emotion_name_map[emotion] }} | {{ metrics.emotion_distribution.get(emotion, 0) }} | {% if emotion_n %}{{ "%.1f"|format((metrics.emotion_distribution.get(emotion, 0) / emotion_n) * 100) }}%{% else %}n/a{% endif %} |
{%- endfor %}

{% if top_emotion %}
**主要发现**：当前已标注子集中，占比最高的情绪是 **{{ emotion_name_map[top_emotion] }}**（{{ metrics.emotion_distribution.get(top_emotion, 0) }} / {{ emotion_n }}）。
{% else %}
**主要发现**：当前尚无可用的探索性情绪标注。
{% endif %}

---

## 七、评估结果、偏差与使用边界

### 7.1 模型对比（动态）

{% for version in metrics.model_versions %}
{% set mm = metrics.per_model[version] %}
- **{{ version }}**：coverage={{ "%.2f"|format(mm.coverage * 100) }}%，accuracy(full)={{ "%.2f"|format(mm.accuracy * 100) }}%，Macro-F1={{ "%.4f"|format(mm.macro_f1) }}，Weighted-F1={{ "%.4f"|format(mm.weighted_f1) }}，errors={{ mm.n_errors }}
{% else %}
- 当前 metrics 中尚无模型结果。
{% endfor %}

### 7.2 关键局限性

1. **样本偏差**：英文社交媒体讨论不代表全部受灾人群。
2. **标签不平衡**：稀有类别指标波动较大。
3. **时间/地点盲点**：无逐帖时间与地点。
4. **非事实核验**：模型预测 ≠ 事实确认。

### 7.3 使用声明

本简报用于课程技术演示。所有数字由程序从冻结数据中计算得出，不可外推为权威灾情评估。

> **不发布预警、救援建议或权威灾情结论。**
> **原始联系方式、姓名、账号和精确住址不在此报告中。**
