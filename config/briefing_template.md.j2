# 课程分析简报 — 2018年 Kerala 洪水社交媒体案例

> 生成时间：{{ generated_at }}
> 数据来源：HumAID `kerala_floods_2018` test split（{{ metrics.total_records }} 条冻结记录）
> 证据状态：📄 数据集记录 / 👤 人工标签 / 🤖 模型预测
> **本简报用于课程技术演示，仅归纳冻结数据中的可见讨论，不代表真实灾情、总体公众需求或权威结论。**

---

## 一、概述

2018年8月，印度 Kerala 邦遭遇百年一遇的特大洪水。灾害期间，社交媒体平台上产生了大量与灾情、求助、救援和捐赠相关的讨论。本简报基于 HumAID 数据集中 Kerala 洪水事件的 1,582 条人工标注英文社交媒体文本，通过传统机器学习 baseline 与 LLM Few-shot 分类器对比，识别九类人道信息，并完成小样本探索性情绪分析。

项目仅验证技术流程和课程概念，不评价真实救援效果，不对外发布灾情结论，也不提供救援调度建议。数据没有逐帖时间和地点信息，所有时空字段固定为 `null`。

---

## 二、数据范围与质量

| 维度 | 数值 |
|------|------|
| 数据来源 | HumAID `kerala_floods_2018` test split |
| 冻结记录数 | {{ metrics.total_records }} |
| 数据字段 | `tweet_id`、`tweet_text`、`class_label` |
| 时间信息 | 无逐帖时间（字段为 null） |
| 地点信息 | 无逐帖地点（字段为 null） |
| 脱敏处理 | 账号 → `[USER]`，号码 → `[NUMBER]`，邮箱 → `[EMAIL]` |
| 正确分类数 | {{ metrics.correct_predictions }} / {{ metrics.total_records }} |
| 准确率 | {{ "%.2f"|format(metrics.accuracy * 100) }}% |

---

## 三、人道信息类别与模型评估

### 3.1 九类人道信息分布

| 类别 | 数量 | 占比 | 精确率 | 召回率 | F1 |
|------|------|------|--------|--------|-----|
{%- for label in labels %}
| {{ label_name_map[label] }} | {{ metrics.category_distribution[label] }} | {{ "%.1f"|format(metrics.category_distribution[label] / metrics.total_records * 100) }}% | {{ "%.3f"|format(metrics.per_class_stats[label].precision) }} | {{ "%.3f"|format(metrics.per_class_stats[label].recall) }} | {{ "%.3f"|format(metrics.per_class_stats[label].f1) }} |
{%- endfor %}

### 3.2 关键类别关注

{% set urgent = metrics.per_class_stats["requests_or_urgent_needs"] %}
- **紧急需求类**：{{ urgent.reference_count }} 条记录，召回率 {{ "%.1f"|format(urgent.recall * 100) }}%。紧急需求是灾害响应中优先级最高的信息类别，其召回率直接影响救援信息能否被及时识别。
{% set rescue = metrics.per_class_stats["rescue_volunteering_or_donation_effort"] %}
- **救援与捐赠类**：{{ rescue.reference_count }} 条记录，在数据中占比最高（{{ "%.1f"|format(rescue.reference_count / metrics.total_records * 100) }}%），反映了灾害期间公众参与志愿和捐赠活动的广泛程度。

---

## 四、代表性脱敏记录

以下为各类别中置信度最高的代表性记录。数据来源已标注，联系方式已脱敏。

{%- for rec in evidence_records %}
{%- if rec.selection_reason.startswith("top-") %}
### {{ label_name_map[rec.predicted_label] }}

- 原文摘要：{{ rec.text_clean }}
- 证据状态：{{ rec.evidence_status }}
- 情感标签：{{ rec.exploratory_emotion or "无" }}
- 置信度：{{ "%.2f"|format(rec.confidence) }}
- 来源：`{{ rec.source_ref }}`
{%- endif %}
{%- endfor %}

---

## 五、可见讨论中的信息缺口

以下基于 HumanAID 九类标签体系的覆盖情况，归纳当前冻结数据中较少被讨论的类别：

{%- for label in gap_labels %}
- **{{ label_name_map[label] }}** 仅 {{ min_count }} 条记录（{{ "%.1f"|format(min_count / metrics.total_records * 100) }}%），可能是数据收集范围偏差，也可能是该信息类别在英文社交媒体上的可见度本身较低。
{%- endfor %}

> 需要说明的是：社交媒体数据不构成对灾情的完整抽样。此处报告的"信息缺口"仅表示冻结数据中该类别的可见讨论较少，不代表实际灾情中不存在此类需求。灾害期间最需要帮助的人群往往不在社交媒体上活跃。

---

## 六、探索性情绪分析

> 本部分情绪标签为课程成员人工标注的小样本探索性实验，不声称为官方 HumAID 标签。

| 情绪类别 | 数量 | 占比 |
|----------|------|------|
{%- for emotion in emotions %}
| {{ emotion_name_map[emotion] }} | {{ metrics.emotion_distribution.get(emotion, 0) }} | {{ "%.1f"|format(metrics.emotion_distribution.get(emotion, 0) / metrics.total_records * 100) }}% |
{%- endfor %}

**主要发现**：
- 积极支持类情绪占比最高，反映社交媒体上救援与捐赠相关的正向讨论较活跃
- 恐慌/焦虑类情绪多集中在紧急需求类别中
- 愤怒类情绪在当前样本中未出现

---

## 七、评估结果、偏差与使用边界

### 7.1 模型对比

本简报中的分类结果来自模型预测。正式评估需在完整 1,582 条 test split 上，比较 TF-IDF + Logistic Regression baseline 与 DeepSeek Few-shot 分类器，报告 Macro/Weighted-F1、逐类 Precision/Recall/Support 和紧急需求类召回率。

### 7.2 关键局限性

1. **样本偏差**：数据来自英文 Twitter，不代表所有受灾人群，尤其无法覆盖不使用社交媒体的群体。
2. **标签不平衡**：部分类别样本极少（如仅 56 条的 `displaced_people_and_evacuations`），稀有类别指标波动较大。
3. **时间盲点**：数据无逐帖时间戳，无法分析信息传播的时间演化。
4. **地点盲点**：数据无逐帖经纬度，无法做空间热点分析。
5. **非事实核验**：模型预测 ≠ 事实确认。每个标签仅表示数据中该类信息的可见性，不代表灾情已核验。

### 7.3 证据类型标识

| 标识 | 含义 |
|------|------|
| 📄 数据集记录 | 数据集中存在该记录，未经进一步确认 |
| 👤 人工标签 | 课程成员完成人工标注 |
| 🤖 模型预测 | 模型输出，未经人工确认 |
| 🔗 外部资料 | 来自独立权威来源 |

### 7.4 使用声明

本简报用于**社会计算与生成决策智能**课程技术演示。所有数字由程序从冻结数据中计算得出，不可外推为灾区总体状况、公众需求统计或权威灾情评估。简报中的"紧急需求""设施损坏"等类别仅反映社交媒体上的可见讨论，不代表已核验的灾情事实。如需做出救援决策，必须以官方权威发布和人工确认的灾情报告为准。

> **不发布预警、救援建议或权威灾情结论。**
> **原始联系方式、姓名、账号和精确住址不在此报告中。**
